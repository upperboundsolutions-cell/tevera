@extends('layouts.app')
@section('title', 'Journey playback & events')
@section('content')
<div class="page-heading"><div><span class="eyebrow accent">JOURNEY HISTORY</span><h1>See the journey unfold.</h1><p class="muted">Replay recorded locations and review server-generated events.</p></div></div>
<section class="panel fleet-section" data-history-url="{{ route('history.data') }}"><form class="fleet-form" data-history-form><div class="fleet-grid"><label>Vehicle<select name="vehicle_id" required><option value="">Select vehicle</option>@foreach($vehicles as $vehicle)<option value="{{ $vehicle->id }}">{{ $vehicle->name }} · {{ $vehicle->registration }}</option>@endforeach</select></label><label>From (UTC)<input type="datetime-local" name="from" value="{{ now()->utc()->subDay()->format('Y-m-d\TH:i') }}" required></label><label>To (UTC)<input type="datetime-local" name="to" value="{{ now()->utc()->format('Y-m-d\TH:i') }}" required></label></div><button class="primary" data-history-load>Load journey</button></form><p class="muted">Maximum 24 hours. Times are UTC (Zimbabwe: UTC +2). Playback steps through recorded fixes; lines between fixes do not prove the exact road travelled.</p>
<p role="status" data-history-status>Choose a vehicle to load its journey.</p><div id="history-map" class="map-canvas" aria-label="Journey playback map"></div><div class="heading-actions"><button type="button" class="primary" data-history-play disabled>Play</button><button type="button" class="secondary" data-history-reset disabled>Restart</button><label>Playback rate<select data-history-speed><option value="1000">1 point / second</option><option value="250">4 points / second</option><option value="100">10 points / second</option></select></label></div><label for="history-timeline">Recorded position<input id="history-timeline" type="range" min="0" max="0" value="0" data-history-slider disabled class="history-slider"></label><p data-history-position>Select a position to see its time and speed.</p><h2>Events & alerts</h2><p class="muted">Events recorded by Traccar, not an independent fault diagnosis. Some alarms require compatible hardware or server rules. This view does not send email or SMS.</p><div class="table-scroll"><table class="fleet-table"><thead><tr><th>Time (UTC)</th><th>Event</th></tr></thead><tbody data-history-events></tbody></table></div></section>
@endsection
